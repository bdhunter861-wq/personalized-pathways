# Copy this file to .env.local and fill in values as they become available.

# Optional: a contact form POST endpoint. If set, the contact form will POST
# the inquiry here as JSON instead of opening the visitor's email client.
# Leave unset to use the free mailto fallback (no paid form service needed).
# NEXT_PUBLIC_CONTACT_ENDPOINT=
